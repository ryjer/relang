void hanoi(string a, string b, string c, int n);
/******************
 * 嵌套块注释 
 * /* 内部一层 */
 *****************/
int main()
{
	int n = 0;
	char ch = 'a';
	+ - * / % × ÷ ++ -- += -= *= ×= ÷= /= %=; //算术运算符
	!= == < <= > >= ≠ ≤ ≥; //比较运算符
	! & | ⊕ ⊙ ~ ^ << >> && || &= |= ⊕= ⊙= ^= <<= >>=; //逻辑运算符位运算符
	∩ ∪ ∈∊ ∉ ∅ Ø ⊂ ⊆ ⊄ ⊈ ⊊ ⫋;
	,.:; ，：；、
	()[]{} （）
	''`"‘’“”
	if (n==1) { //你好
		hanoi("A", "B", "C", n);
	}
	return 0;
} 190 0xfF 0ha0 0o77 0b1111 'a' ‘永’  '\t' '\n' 'b 'k 0h 0o 0b