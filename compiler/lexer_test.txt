void hanoi(string a, string b, string c, int n);
/******************
 * 嵌套块注释 
 * /* 内部一层 */
 *****************/
int main()
{
	int n = 0;
	char ch = 'a';
	+ - * / % × ÷ ++ -- += -= *= ×= ÷= /= %=; //算术运算符
	!= == < <= > >= ≠ ≤ ≥; //比较运算符
	! & | ⊕ ⊙ ~ ^ << >> && || &= |= ⊕= ⊙= ^= <<= >>=; //逻辑运算符位运算符
	∩ ∪ ∈∊ ∉ ∅ Ø ⊂ ⊆ ⊄ ⊈ ⊊ ⫋;
	,.:; ，：；、。? ？ :: := 
	()[]{} （）
	''`"‘’“”
	// 有向箭头
	<- <-> -> ← ↔ → <= <=> => ⇔ ⇒ <| |>  
	if (n==1) { //你好
		hanoi("A", "B", "C", n);
	}
	return 0; //返回0
} 0 09 190 0d09 0x0Ff 0ha0 0o07 0b0011 'a' ‘永’  '\t' '\n' 'b ' \
“求: '3/2'的值 \\ \n”  