void hanoi(string a, string b, string c, int n);

int main()
{
	int n = -1;
	+ - * / % × ÷ ++ -- += -= *= ×= ÷= /= %=; //算术运算符
	!= == < <= > >= ≠ ≤ ≥; //比较运算符
	! & | ⊕ ⊙ ~ ^ << >> && || &= |= ⊕= ⊙= ^= <<= >>=; //逻辑运算符位运算符
	∩ ∪ ∈∊ ∉ Ø ⊂ ⊆ ⊄ ⊈ ⊊ ⫋;
	,.:; ，：；
	()[]{} （）
	'`"‘’“”
	if (n==1) { //你好
		hanoi("A", "B", "C", n);
	}
	return 0;
} 0xfF 0h0a 0o77 0b1111